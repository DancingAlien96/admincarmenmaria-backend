# Inventario de Ansible. El control y el destino son el MISMO servidor (localhost),
# por eso usamos connection=local. Los playbooks corren en el propio VPS.
[carmenmaria]
localhost ansible_connection=local
